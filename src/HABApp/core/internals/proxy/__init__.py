from .proxy_obj import ConstProxyObj, create_proxy


# isort: split

from .proxies import (
    setup_internals,
    uses_event_bus,
    uses_file_manager,
    uses_get_item,
    uses_item_registry,
    uses_post_event,
)
